# KISS deck files

Committed layouts (the Sharing section of the Machine Sections pattern). Each `.json` is one portable deck — move it, share it, or load it from the deck toolbar. Representation lives here only when you explicitly commit it.
