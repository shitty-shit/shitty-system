# Drafts (STFU+WRITE)

Source drafts are immutable until you explicitly save from the WRITE cube (Ctrl+S / 💾). Exports land in `export/` as .md + .txt. The KISS frame never overwrites these silently.
