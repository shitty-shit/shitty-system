# Freebuff Memory Setup Receipt

Date: 2026-08-16
Owner: Kevin Kane
Scope: Freebuff chats operating in `C:\Users\idfk\Desktop\harness`

## Intent

Create one local, repo-backed IJFW memory store for continuity across Freebuff
chats and their agents. Other IDEs and system-wide AI clients are out of scope
for this first proof.

## Changes

- Created the dedicated `freebuff-memory` repository folder.
- Defined the memory policy and chat start/checkpoint/handoff contract.
- Configured Freebuff/Codex to launch IJFW's local MCP server.
- Pinned `IJFW_PROJECT_DIR` to this repository to prevent split memory.
- Auto-approved only IJFW's memory and prompt-check tools; execution, update,
  state, brain, and audit tools retain approval prompts.
- Added harness-level instructions that require IJFW recall and handoffs.
- Installed IJFW's declared runtime dependencies, which were absent from the
  local installation.
- Fixed IJFW natural-language recall to await its asynchronous search function.

## Backups

- `C:\Users\idfk\.codex\config.toml.backup-freebuff-memory-20260816-222814`
- `C:\Users\idfk\.ijfw\mcp-server\src\server.js.backup-freebuff-recall-20260816-223016`

## Reversal

1. Remove the `[mcp_servers.ijfw-memory]` and
   `[mcp_servers.ijfw-memory.env]` sections from `~/.codex/config.toml`, or
   restore the timestamped backup created during setup.
2. Remove `C:\Users\idfk\Desktop\harness\AGENTS.md` if its only purpose is
   this memory integration.
3. Archive or remove this repository only after preserving any wanted memory.

## Verification target

One chat stores a canary and handoff; a new chat recalls both from this repo
after a complete Freebuff restart.

## Verification performed

- Codex parsed `~/.codex/config.toml` and listed `ijfw-memory` as enabled.
- IJFW accepted decision, preference, observation, and handoff writes.
- A fresh MCP process recalled `FREEBUFF-CROSSCHAT-CANARY-20260816` through
  the summary prelude.
- `scripts/verify.ps1` passed its transport, prelude, and canary checks.
- IJFW smoke assertions reported 104/104 passing. The smoke command still
  exits nonzero on Windows because its unrelated symlink-security test cannot
  create `C:\etc\passwd` (`EPERM`).
